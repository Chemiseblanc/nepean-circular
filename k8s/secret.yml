# TEMPLATE ONLY — not applied by CI. Create the secret manually once:
#
#   kubectl create secret generic nepean-circular-secrets \
#     --from-literal=SECRET_KEY_BASE="$(mix phx.gen.secret)" \
#     --from-literal=POSTMARK_API_KEY="your-postmark-api-key" \
#     -n nepean-circular
#
# To rotate a value:
#
#   kubectl create secret generic nepean-circular-secrets \
#     --from-literal=SECRET_KEY_BASE="new-value" \
#     --from-literal=POSTMARK_API_KEY="new-value" \
#     -n nepean-circular \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# DO NOT commit real secret values to version control.

apiVersion: v1
kind: Secret
metadata:
  name: nepean-circular-secrets
  namespace: nepean-circular
type: Opaque
stringData:
  SECRET_KEY_BASE: "REPLACE_ME"
  POSTMARK_API_KEY: "REPLACE_ME"
